[project]
name = "d_classified"
version = "0.1.0"
description = "Portfolio ingestion + Airflow pipeline"
authors = [{ name = "Joey" }]
dependencies = []

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["scripts"]